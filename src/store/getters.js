
const getters = {
  conditionParam: state => state.conditionParam,
  defaultParam: state => state.defaultParam,
  roleArr: state => state.roleArr,
  activeStatus: state => state.activeStatus
}

export default getters
